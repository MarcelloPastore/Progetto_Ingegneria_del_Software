export class SpeseService {
    static async getSaldo(idUtente: string, idCasa: string): Promise<number> {
        //TODO: Implementare il calcolo del saldo
        return 0;
    }

    static async getSaldoPerCoinquilino(idUtente: string, idCoinquilino: string, idCasa: string): Promise<number> {
        //TODO: Implementare il calcolo del saldo per coinquilino
        return 0;
    }
}