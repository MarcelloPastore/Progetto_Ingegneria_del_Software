import 'package:flutter/material.dart';
import 'app.dart';

void main() {
  runApp(const CoinCasaApp());
}

// per visualizzare la schermata di account attivato dopo la conferma dell'email, eseguire il comando seguente da terminale:
//flutter run -d <device_id> --dart-define=SHOW_ACCOUNT_ACTIVATED=true